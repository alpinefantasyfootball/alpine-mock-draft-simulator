{
  "name": "juke-worker",
  "private": true,
  "version": "0.0.0",
  "description": "The Cloudflare Worker behind an invite link, the two proxied routes, and Clerk session verification. wrangler bundles this itself on dev/deploy; this file exists so it has a real dependency (@clerk/backend) to bundle.",
  "type": "module",
  "dependencies": {
    "@clerk/backend": "^3.16.13"
  }
}
