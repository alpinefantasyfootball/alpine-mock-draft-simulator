{
  "name": "juke-tests",
  "private": true,
  "description": "Dev-only. Fetches the end-to-end test runner, which drives the real pages in a real browser (built via web/, the one part of the site with a real build step — see CLAUDE.md's Stack section). Nothing here is served, imported or required by the site.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
